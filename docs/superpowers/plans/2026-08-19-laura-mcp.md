# Laura-MCP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ein stdio-MCP-Server (`services/mcp`), der die komplette Laura-App (Import, Analyse, Produktion mit Gates, Schnitt, Export) für Claude Code und Claude Desktop bedienbar macht, plus die Backend-Endpunkte für den Author-Modus (externe Session schreibt Szenen-Vorschlag/Storyline/Script mit denselben serverseitigen Guards wie das Team).

**Architecture:** Der MCP-Server ist ein reiner HTTP-Client gegen die laufende App auf `http://127.0.0.1:8765` — er importiert keinen Backend-Code. Die Author-Schreibpfade laufen über neue FastAPI-Endpunkte, die die **bestehenden Team-Tool-Closures** aus `build_production_tool_specs` per Namens-Dispatch aufrufen (`short_creator/authoring.py`); dadurch gibt es genau eine Guard-Quelle. *(Bewusste Mechanik-Abweichung von der Spec-Formulierung „Kernlogik in Service-Funktionen ziehen": statt ~500 Zeilen Tool-Bodies zu verschieben, ruft `authoring.py` die Factory auf und dispatcht per Tool-Name — dasselbe Ziel, kein doppelter Guard-Code, ein Bruchteil des Risikos.)* Der Approve-Kern wird aus dem Chat-Executor in einen Service in `api/short_creator.py` gezogen, den Executor **und** neuer Endpunkt teilen.

**Tech Stack:** Python 3.11+/uv, `mcp` (offizielles Python-SDK, FastMCP), `httpx` (+ `httpx.MockTransport` in Tests), pytest, mypy strict, ruff. Backend: bestehendes FastAPI/SQLite/Board-Gefüge.

**Spec:** `docs/superpowers/specs/2026-08-19-laura-mcp-design.md`

## Global Constraints

- Base-URL ist fest `http://127.0.0.1:8765` — nicht konfigurierbar. Token kommt aus Env `LAURA_TOKEN` und wandert als Header `X-Laura-Token` mit.
- Backend nicht erreichbar → exakt dieser Fehlertext aus jedem Tool: `Laura app is not running — start the Laura desktop app, then retry.`
- **stdio-Disziplin:** Der MCP-Prozess darf NIE auf stdout schreiben (stdout ist der Protokollkanal). Logging ausschließlich über `logging` (stderr). Kein `print` (Repo-Regel gilt ohnehin).
- HTTP-Fehler geben den `detail`-Satz des Backends weiter, nie das rohe JSON.
- Zeitkern-Invarianten in jeder Editorial-Tool-Beschreibung: Ganzzahl-Frames, Ranges end-exclusive (`out_frame_exclusive`), OTIO ist Source of Truth.
- Author-Sessions: `scene_gate=True` und `script_gate=True` erzwungen; `author="external"` in `BoardMeta`; Team-Pfad (`/production/{sid}/message`) und Author-Schreibpfade schließen sich gegenseitig mit 409 aus.
- `services/local-api` bekommt KEINE neuen Abhängigkeiten; schwere Modelle bleiben optionale Extras.
- Python: mypy strict + ruff clean in beiden Paketen; Tests mit pytest. Conventional Commits; `git add` immer mit expliziten Pfaden.
- MCP-Tool-Zahl und -Namen exakt wie die Spec-Tabellen (26 Tools + `laura_api`).

## File Structure

```
services/mcp/
  pyproject.toml               # eigenes uv-Projekt "laura-mcp"
  README.md                    # Registrierung Claude Code + Claude Desktop
  src/laura_mcp/
    __init__.py
    client.py                  # LauraClient (httpx), Fehler-Mapping, get_bytes
    server.py                  # FastMCP-Instanz, register()-Aufrufe, main()
    tools_media.py             # list_projects, list_assets, import_media, import_status
    tools_analysis.py          # analyze_asset, get_transcript, get_shots_and_scenes, search_material
    tools_vision.py            # get_frame, get_contact_sheet (Image-Content)
    tools_production.py        # start_production … approve_script (7 Tools)
    tools_editorial.py         # get_timeline, edit_timeline, edit_scenes, timeline_undo
    tools_export.py            # render_timeline, list_exports, get_export, auto_produce
    tools_jobs.py              # job_status (mit wait_s)
    tools_raw.py               # laura_api Notausgang
  tests/
    conftest.py                # make_client(handler) → LauraClient mit MockTransport
    test_client.py
    test_tools_media.py
    test_tools_analysis.py
    test_tools_vision.py
    test_tools_production.py
    test_tools_editorial.py
    test_tools_export.py
    test_tools_jobs_raw.py

services/local-api/src/laura/
  short_creator/authoring.py   # NEU: call_production_tool Dispatch + Guards
  short_creator/board_models.py# BoardMeta.author Feld
  api/short_creator.py         # author-Flag im Create, 3 PUT-Endpunkte, approve-Service+Endpunkt,
                               # Follow-up-Sperre für Author-Sessions
  chat/executor.py             # _handle_approve_script ruft den extrahierten Service

services/local-api/tests/
  test_authoring.py            # NEU: Dispatch + Guards
  test_api_author_mode.py      # NEU: Create/Sperren/Approve

.claude/skills/laura-producer/SKILL.md   # Produktions-Vertrag für treibende Sessions
.github/workflows/ci.yml       # neuer Job "mcp"
```

Hinweis für alle MCP-Tasks: Tool-Funktionen sind **synchron** (FastMCP führt sync-Funktionen im Threadpool aus) — kein async/await nötig, und die Tests bleiben einfache pytest-Funktionen.

---

### Task 1: services/mcp Grundgerüst — LauraClient, Server-Skelett, laura_api

**Files:**
- Create: `services/mcp/pyproject.toml`
- Create: `services/mcp/src/laura_mcp/__init__.py`
- Create: `services/mcp/src/laura_mcp/client.py`
- Create: `services/mcp/src/laura_mcp/server.py`
- Create: `services/mcp/src/laura_mcp/tools_raw.py`
- Create: `services/mcp/tests/conftest.py`
- Test: `services/mcp/tests/test_client.py`

**Interfaces:**
- Produces: `LauraClient(token: str, transport: httpx.BaseTransport | None = None)` mit
  `request(method: str, path: str, *, json: Any = None, params: dict[str, Any] | None = None, timeout: float | None = None) -> Any` (geparstes JSON; wirft `LauraError`),
  `get_bytes(path: str) -> bytes`, Konstante `BACKEND_DOWN = "Laura app is not running — start the Laura desktop app, then retry."`, Exception `LauraError(Exception)` mit `.message`.
- Produces: `server.py` mit `mcp = FastMCP("laura")`, `build_client() -> LauraClient` (liest `LAURA_TOKEN`, wirft `LauraError` bei fehlendem Token), `main() -> None` (registriert alle Familien, `mcp.run()`), und Registrier-Konvention `register(mcp: FastMCP, client: LauraClient) -> None` pro Familienmodul.
- Produces: Tool `laura_api(method, path, body=None)`.

- [ ] **Step 1: pyproject anlegen**

```toml
[project]
name = "laura-mcp"
version = "0.1.0"
description = "MCP server exposing the local Laura app (127.0.0.1:8765) to Claude sessions."
requires-python = ">=3.11"
dependencies = ["mcp>=1.2", "httpx>=0.27"]

[project.scripts]
laura-mcp = "laura_mcp.server:main"

[dependency-groups]
dev = ["pytest>=8", "mypy>=1.10", "ruff>=0.5"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/laura_mcp"]

[tool.mypy]
strict = true
python_version = "3.11"

[tool.ruff]
line-length = 100
```

- [ ] **Step 2: Failing Test für das Fehler-Mapping schreiben**

`services/mcp/tests/conftest.py`:

```python
"""MockTransport plumbing: every test builds a LauraClient whose HTTP layer is a local handler."""
from __future__ import annotations

from typing import Any, Callable

import httpx

from laura_mcp.client import LauraClient


def make_client(handler: Callable[[httpx.Request], httpx.Response]) -> LauraClient:
    return LauraClient(token="test-token", transport=httpx.MockTransport(handler))
```

`services/mcp/tests/test_client.py`:

```python
from __future__ import annotations

import httpx
import pytest

from laura_mcp.client import BACKEND_DOWN, LauraClient, LauraError

from .conftest import make_client


def test_request_sends_token_and_parses_json() -> None:
    seen: dict[str, str] = {}

    def handler(request: httpx.Request) -> httpx.Response:
        seen["token"] = request.headers.get("X-Laura-Token", "")
        seen["url"] = str(request.url)
        return httpx.Response(200, json={"ok": True})

    client = make_client(handler)
    assert client.request("GET", "/projects") == {"ok": True}
    assert seen["token"] == "test-token"
    assert seen["url"] == "http://127.0.0.1:8765/projects"


def test_http_error_surfaces_detail_sentence_not_raw_json() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(404, json={"detail": "session not found"})

    client = make_client(handler)
    with pytest.raises(LauraError) as exc:
        client.request("GET", "/production/nope")
    assert "session not found" in str(exc.value)
    assert "{" not in str(exc.value)  # never the raw JSON body


def test_connect_error_becomes_backend_down_message() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        raise httpx.ConnectError("refused")

    client = make_client(handler)
    with pytest.raises(LauraError) as exc:
        client.request("GET", "/projects")
    assert str(exc.value) == BACKEND_DOWN


def test_get_bytes_returns_raw_body() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(200, content=b"\x89PNG...")

    client = make_client(handler)
    assert client.get_bytes("/assets/a/frame/10").startswith(b"\x89PNG")
```

- [ ] **Step 3: Test laufen lassen — muss fehlschlagen**

Run: `cd services/mcp && uv run pytest tests/test_client.py -v`
Expected: FAIL (`ModuleNotFoundError: laura_mcp`)

- [ ] **Step 4: client.py implementieren**

```python
"""HTTP client for the running Laura app. Pure client — never imports backend code."""
from __future__ import annotations

import logging
from typing import Any

import httpx

logger = logging.getLogger(__name__)

BASE_URL = "http://127.0.0.1:8765"  # deliberately hardwired: local app only
BACKEND_DOWN = "Laura app is not running — start the Laura desktop app, then retry."
DEFAULT_TIMEOUT = 30.0


class LauraError(Exception):
    """One human-readable sentence per failure; MCP tools surface str(exc) verbatim."""


class LauraClient:
    def __init__(self, token: str, transport: httpx.BaseTransport | None = None) -> None:
        self._http = httpx.Client(
            base_url=BASE_URL,
            headers={"X-Laura-Token": token},
            timeout=DEFAULT_TIMEOUT,
            transport=transport,
        )

    def request(
        self,
        method: str,
        path: str,
        *,
        json: Any = None,
        params: dict[str, Any] | None = None,
        timeout: float | None = None,
    ) -> Any:
        response = self._send(method, path, json=json, params=params, timeout=timeout)
        if not response.content:
            return None
        return response.json()

    def get_bytes(self, path: str, *, timeout: float | None = None) -> bytes:
        return self._send("GET", path, json=None, params=None, timeout=timeout).content

    def _send(
        self,
        method: str,
        path: str,
        *,
        json: Any,
        params: dict[str, Any] | None,
        timeout: float | None,
    ) -> httpx.Response:
        try:
            response = self._http.request(
                method, path, json=json, params=params,
                timeout=timeout if timeout is not None else DEFAULT_TIMEOUT,
            )
        except httpx.ConnectError as exc:
            raise LauraError(BACKEND_DOWN) from exc
        except httpx.HTTPError as exc:  # timeouts, protocol errors
            raise LauraError(f"Laura request failed: {exc}") from exc
        if response.is_success:
            return response
        raise LauraError(_detail_sentence(response))


def _detail_sentence(response: httpx.Response) -> str:
    """The backend's detail sentence, never the raw JSON (same rule as the desktop client)."""
    try:
        payload = response.json()
    except ValueError:
        payload = None
    if isinstance(payload, dict):
        detail = payload.get("detail")
        if isinstance(detail, str) and detail:
            return detail
        if detail is not None:
            return str(detail)
    return f"HTTP {response.status_code} from the Laura backend"
```

- [ ] **Step 5: server.py + tools_raw.py implementieren**

`src/laura_mcp/server.py`:

```python
"""stdio MCP server for the Laura app. stdout is the protocol channel — log to stderr only."""
from __future__ import annotations

import logging
import os

from mcp.server.fastmcp import FastMCP

from .client import LauraClient, LauraError

logging.basicConfig(level=logging.INFO)  # stderr by default; NEVER print to stdout

mcp = FastMCP("laura")


def build_client() -> LauraClient:
    token = os.environ.get("LAURA_TOKEN", "")
    if not token:
        raise LauraError("LAURA_TOKEN is not set — configure it in the MCP registration env.")
    return LauraClient(token=token)


def main() -> None:
    from . import (
        tools_analysis,
        tools_editorial,
        tools_export,
        tools_jobs,
        tools_media,
        tools_production,
        tools_raw,
        tools_vision,
    )

    client = build_client()
    for module in (
        tools_media, tools_analysis, tools_vision, tools_production,
        tools_editorial, tools_export, tools_jobs, tools_raw,
    ):
        module.register(mcp, client)
    mcp.run()
```

(Bis die Familienmodule existieren, legt dieser Task von jedem nur `tools_raw.py` an; die
Imports der übrigen Module kommen mit ihren Tasks dazu — `main()` startet in diesem Task also
nur mit `tools_raw.register(...)` und die anderen Import-/Loop-Einträge bleiben auskommentiert
mit `# Task N`.)

`src/laura_mcp/tools_raw.py`:

```python
"""laura_api — the escape hatch for every endpoint without a first-class tool."""
from __future__ import annotations

from typing import Any

from mcp.server.fastmcp import FastMCP

from .client import LauraClient


def register(mcp: FastMCP, client: LauraClient) -> None:
    @mcp.tool()
    def laura_api(method: str, path: str, body: dict[str, Any] | None = None) -> Any:
        """Raw call against the local Laura API (127.0.0.1:8765) for endpoints without a
        dedicated tool (reels, per-scene music, audio clips, voiceover voices, batch shorts,
        sequence transitions, deletes ...). method is GET/POST/PUT/PATCH/DELETE; path starts
        with '/'. DESTRUCTIVE CALLS (any DELETE, anything ending in a removal): always ask
        the user for confirmation first — deleting productions/assets/projects is permanent.
        """
        return client.request(method.upper(), path, json=body)
```

- [ ] **Step 6: `__init__.py` (leer) anlegen, Tests laufen lassen**

Run: `cd services/mcp && uv sync && uv run pytest tests/ -v`
Expected: 4 PASS

- [ ] **Step 7: Gates + Commit**

Run: `cd services/mcp && uv run mypy src tests && uv run ruff check src tests`
Expected: clean

```bash
git add services/mcp
git commit -m "feat(mcp): laura-mcp package skeleton — LauraClient, server entry, laura_api escape hatch"
```

---

### Task 2: Media-Familie (4 Tools)

**Files:**
- Create: `services/mcp/src/laura_mcp/tools_media.py`
- Modify: `services/mcp/src/laura_mcp/server.py` (Import + register-Eintrag aktivieren)
- Test: `services/mcp/tests/test_tools_media.py`

**Interfaces:**
- Consumes: `LauraClient.request`, `register(mcp, client)`-Konvention aus Task 1.
- Produces: Tools `list_projects()`, `list_assets(project_id)`, `import_media(project_id, source, display_name=None, format=None, cookies_from_browser=None)`, `import_status(asset_id)`.
- Endpunkt-Fakten: `POST /projects/{pid}/assets/import` nimmt `{source_path}` ODER `{source_url}` (+ optional `display_name`, `format`, `cookies_from_browser`) und antwortet 202 `{asset_id, job_id, extra_asset_ids?}` (Playlist-Fan-out). `GET /projects` listet Projekte, `GET /projects/{pid}/assets` Assets, `GET /assets/{aid}/import-status` den Fortschritt.

**Test-Muster für ALLE Tool-Familien** (hier einmal ausgeschrieben, die folgenden Tasks
verwenden wörtlich dasselbe Gerüst mit anderen Assertions): Der Test registriert die Familie
auf einer frischen `FastMCP`-Instanz, holt das Tool über `mcp._tool_manager.get_tool(name).fn`
— falls diese private Stelle in der installierten mcp-Version anders heißt, stattdessen die
Funktionen im Modul über eine Modul-Ebene-Registry greifen (siehe Step 2, `_TOOLS`-Dict) —
und ruft es direkt auf; der MockTransport-Handler prüft Methode/Pfad/Body.

- [ ] **Step 1: Failing Tests**

`services/mcp/tests/test_tools_media.py`:

```python
from __future__ import annotations

import json
from typing import Any

import httpx
import pytest

from laura_mcp import tools_media
from laura_mcp.client import LauraError

from .conftest import make_client


def call(tool: str, handler: Any, /, **kwargs: Any) -> Any:
    client = make_client(handler)
    return tools_media.TOOLS[tool](client, **kwargs)


def test_list_projects_hits_projects_endpoint() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        assert request.method == "GET" and request.url.path == "/projects"
        return httpx.Response(200, json=[{"id": "p1", "name": "Demo"}])

    assert call("list_projects", handler) == [{"id": "p1", "name": "Demo"}]


def test_import_media_with_url_posts_source_url() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        assert request.method == "POST"
        assert request.url.path == "/projects/p1/assets/import"
        body = json.loads(request.content)
        assert body == {"source_url": "https://example.com/v.mp4", "format": "best"}
        return httpx.Response(202, json={"asset_id": "a1", "job_id": "j1"})

    out = call("import_media", handler, project_id="p1",
               source="https://example.com/v.mp4", format="best")
    assert out["asset_id"] == "a1"


def test_import_media_with_path_posts_source_path() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        body = json.loads(request.content)
        assert body == {"source_path": "C:/videos/talk.mp4"}
        return httpx.Response(202, json={"asset_id": "a2", "job_id": "j2"})

    assert call("import_media", handler, project_id="p1",
                source="C:/videos/talk.mp4")["asset_id"] == "a2"


def test_import_status_path() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        assert request.url.path == "/assets/a1/import-status"
        return httpx.Response(200, json={"phase": "downloading"})

    assert call("import_status", handler, asset_id="a1")["phase"] == "downloading"


def test_backend_down_message_reaches_caller() -> None:
    def handler(request: httpx.Request) -> httpx.Response:
        raise httpx.ConnectError("refused")

    with pytest.raises(LauraError, match="Laura app is not running"):
        call("list_projects", handler)
```

- [ ] **Step 2: Run — FAIL** (`AttributeError: TOOLS`), dann implementieren

`src/laura_mcp/tools_media.py` — Muster: jede Familie hält ihre Kernfunktionen in einem
testbaren `TOOLS`-Dict (`name -> fn(client, **kwargs)`); `register` bindet sie als
`@mcp.tool()`-Closures an den echten Client. Dieses Muster gilt wörtlich für alle
Familienmodule dieses Plans:

```python
"""Media family: projects, assets, import (file or URL), import progress."""
from __future__ import annotations

from typing import Any, Callable

from mcp.server.fastmcp import FastMCP

from .client import LauraClient


def _list_projects(client: LauraClient) -> Any:
    return client.request("GET", "/projects")


def _list_assets(client: LauraClient, *, project_id: str) -> Any:
    return client.request("GET", f"/projects/{project_id}/assets")


def _import_media(
    client: LauraClient,
    *,
    project_id: str,
    source: str,
    display_name: str | None = None,
    format: str | None = None,
    cookies_from_browser: str | None = None,
) -> Any:
    is_url = source.startswith(("http://", "https://"))
    body: dict[str, Any] = {"source_url": source} if is_url else {"source_path": source}
    if display_name:
        body["display_name"] = display_name
    if format:
        body["format"] = format
    if cookies_from_browser:
        body["cookies_from_browser"] = cookies_from_browser
    return client.request("POST", f"/projects/{project_id}/assets/import", json=body)


def _import_status(client: LauraClient, *, asset_id: str) -> Any:
    return client.request("GET", f"/assets/{asset_id}/import-status")


TOOLS: dict[str, Callable[..., Any]] = {
    "list_projects": _list_projects,
    "list_assets": _list_assets,
    "import_media": _import_media,
    "import_status": _import_status,
}


def register(mcp: FastMCP, client: LauraClient) -> None:
    @mcp.tool()
    def list_projects() -> Any:
        """List all Laura projects (id, name, frame rate)."""
        return _list_projects(client)

    @mcp.tool()
    def list_assets(project_id: str) -> Any:
        """List a project's media assets (id, display name, online, duration)."""
        return _list_assets(client, project_id=project_id)

    @mcp.tool()
    def import_media(
        project_id: str,
        source: str,
        display_name: str | None = None,
        format: str | None = None,
        cookies_from_browser: str | None = None,
    ) -> Any:
        """Import media into a project. `source` is a local file path OR a URL (yt-dlp;
        playlist/channel URLs fan out into one asset per entry — the response's
        extra_asset_ids lists the rest). Import runs async: poll import_status(asset_id).
        """
        return _import_media(
            client, project_id=project_id, source=source, display_name=display_name,
            format=format, cookies_from_browser=cookies_from_browser,
        )

    @mcp.tool()
    def import_status(asset_id: str) -> Any:
        """Progress of a running import (phase, percent, error)."""
        return _import_status(client, asset_id=asset_id)
```

- [ ] **Step 3: server.py-Import aktivieren, Tests laufen lassen**

Run: `cd services/mcp && uv run pytest tests/test_tools_media.py -v`
Expected: 5 PASS

- [ ] **Step 4: Gates + Commit**

```bash
git add services/mcp/src/laura_mcp/tools_media.py services/mcp/src/laura_mcp/server.py services/mcp/tests/test_tools_media.py
git commit -m "feat(mcp): media family — list_projects/list_assets/import_media/import_status"
```

---

### Task 3: Analyse & Suche (4 Tools)

**Files:**
- Create: `services/mcp/src/laura_mcp/tools_analysis.py`
- Modify: `services/mcp/src/laura_mcp/server.py`
- Test: `services/mcp/tests/test_tools_analysis.py`

**Interfaces:**
- Produces: `analyze_asset(asset_id)`, `get_transcript(asset_id, start_frame=None, end_frame_exclusive=None)`, `get_shots_and_scenes(project_id, asset_id)`, `search_material(project_id, query, mode="semantic", limit=10)`.
- Endpunkt-Fakten: `POST /assets/{aid}/analysis` (Body `{}`) → job; `GET /assets/{aid}/transcript`; `GET /assets/{aid}/shots`; `GET /projects/{pid}/assets/{aid}/rough-cut`; `POST /search` mit `{project_id, query, mode, limit}` → Liste `{segment_id, asset_id, asset_name, start_frame, end_frame, text, speaker_label, score}`.

Implementierung nach dem `TOOLS`-Dict-Muster aus Task 2. Besonderheiten:

- `get_transcript` filtert client-seitig auf den Frame-Bereich, wenn `start_frame`/`end_frame_exclusive` gesetzt sind: behalte nur Segmente mit `segment["end_frame"] > start_frame and segment["start_frame"] < end_frame_exclusive` (halboffene Überlappung). Tool-Beschreibung: „Frames are integers; ranges are end-exclusive."
- `get_shots_and_scenes` macht zwei GETs und liefert `{"shots": ..., "rough_cut": ...}`.
- `analyze_asset` postet `{}` als Body.

- [ ] **Step 1: Failing Tests** — dasselbe Gerüst wie `test_tools_media.py`; Fälle:
  `test_analyze_asset_posts_analysis` (POST `/assets/a1/analysis`),
  `test_get_transcript_plain` (GET `/assets/a1/transcript`),
  `test_get_transcript_filters_frame_range` (Handler liefert 3 Segmente `[0,100)`, `[100,200)`, `[200,300)`; Aufruf mit `start_frame=100, end_frame_exclusive=200` → genau das mittlere),
  `test_get_shots_and_scenes_merges_two_gets` (beide Pfade werden getroffen, Ergebnis hat Schlüssel `shots` und `rough_cut`),
  `test_search_material_posts_query` (Body `{"project_id": "p1", "query": "pricing", "mode": "semantic", "limit": 10}`).
- [ ] **Step 2: Run — FAIL, dann `tools_analysis.py` implementieren** (Muster Task 2; vier `_`-Funktionen + `TOOLS` + `register` mit den Docstrings inkl. Frame-Invarianten).
- [ ] **Step 3: Run — PASS; server.py-Import aktivieren**
- [ ] **Step 4: Gates + Commit**

```bash
git add services/mcp/src/laura_mcp/tools_analysis.py services/mcp/src/laura_mcp/server.py services/mcp/tests/test_tools_analysis.py
git commit -m "feat(mcp): analysis family — analyze/transcript/shots+scenes/semantic search"
```

---

### Task 4: Sehen (Image-Content) + Jobs

**Files:**
- Create: `services/mcp/src/laura_mcp/tools_vision.py`
- Create: `services/mcp/src/laura_mcp/tools_jobs.py`
- Modify: `services/mcp/src/laura_mcp/server.py`
- Test: `services/mcp/tests/test_tools_vision.py`, `services/mcp/tests/test_tools_jobs_raw.py`

**Interfaces:**
- Produces: `get_frame(asset_id, frame)` und `get_contact_sheet(session_id)` — beide geben `mcp.server.fastmcp.Image` zurück (`Image(data=png_bytes, format="png")`); `job_status(job_id, wait_s=0)`.
- Endpunkt-Fakten: `GET /assets/{aid}/frame/{frame}` → PNG-Bytes; `GET /production/{sid}/contact-sheet` → PNG-Bytes; `GET /jobs/{jid}` → `{status, ...}` mit `status ∈ queued|running|succeeded|failed|cancelled`.

Besonderheiten:
- Die `_`-Kernfunktionen geben **bytes** zurück (testbar ohne MCP-Typen); erst die
  `register`-Closures wickeln sie in `Image(...)`. Sollte `mcp.server.fastmcp.Image` in der
  installierten Version nicht existieren, stattdessen
  `mcp.types.ImageContent(type="image", data=base64.b64encode(png).decode(), mimeType="image/png")`
  zurückgeben — beides prüft der Implementer gegen das installierte Paket, NICHT raten.
- `job_status` mit `wait_s > 0` pollt serverseitig: Schleife `while time.monotonic() < deadline`,
  bei nicht-terminalem Status `time.sleep(1.0)`; `wait_s` hart auf `min(wait_s, 60)` gekappt.
  Terminal sind `succeeded|failed|cancelled`.

- [ ] **Step 1: Failing Tests** — Fälle:
  `test_get_frame_returns_png_bytes` (Handler liefert `b"\x89PNG..."`, Kernfunktion gibt exakt die Bytes),
  `test_get_contact_sheet_path` (Pfad `/production/s1/contact-sheet`),
  `test_job_status_no_wait_single_get`,
  `test_job_status_wait_polls_until_terminal` (Handler-Closure zählt Aufrufe: 1→`running`, 2→`succeeded`; `job_status(..., wait_s=5)` liefert `succeeded` nach 2 GETs; `time.sleep` per `monkeypatch.setattr(tools_jobs.time, "sleep", lambda s: None)` neutralisiert),
  `test_job_status_wait_caps_at_60` (Aufruf mit `wait_s=600`: gepatchtes `time.monotonic` beweist, dass die Deadline ≤ Start+60 liegt).
- [ ] **Step 2: Run — FAIL, dann implementieren** (Muster Task 2).
- [ ] **Step 3: Run — PASS; server.py-Imports aktivieren**
- [ ] **Step 4: Gates + Commit**

```bash
git add services/mcp/src/laura_mcp/tools_vision.py services/mcp/src/laura_mcp/tools_jobs.py services/mcp/src/laura_mcp/server.py services/mcp/tests/test_tools_vision.py services/mcp/tests/test_tools_jobs_raw.py
git commit -m "feat(mcp): vision family (real PNG frames/contact sheets) + job_status with bounded wait"
```

---

### Task 5: Editorial + Render & Export (8 Tools)

**Files:**
- Create: `services/mcp/src/laura_mcp/tools_editorial.py`
- Create: `services/mcp/src/laura_mcp/tools_export.py`
- Modify: `services/mcp/src/laura_mcp/server.py`
- Test: `services/mcp/tests/test_tools_editorial.py`, `services/mcp/tests/test_tools_export.py`

**Interfaces:**
- Produces (editorial): `get_timeline(timeline_id=None, project_id=None)`, `edit_timeline(timeline_id, operation)`, `edit_scenes(timeline_id, action, args)`, `timeline_undo(timeline_id, redo=False)`.
- Produces (export): `render_timeline(timeline_id)`, `list_exports(project_id)`, `get_export(export_id)`, `auto_produce(kind, project_id, topic, target_seconds=60)`.
- Endpunkt-Fakten:
  - `GET /timelines/{tid}` bzw. `GET /projects/{pid}/timelines`; History: `GET /timelines/{tid}/history`.
  - `POST /timelines/{tid}/operations` — Body ist `{"op": <name>, ...opspezifische Felder...}` mit `op ∈ {trim, move, delete, delete_words, lift, insert, append, place_clip, set_speed, set_audio_offset}`; `edit_timeline` reicht das `operation`-Dict 1:1 durch (validiert nur, dass `operation["op"]` in der Liste ist — die Feld-Validierung macht das Backend).
  - Szenen: `POST /timelines/{tid}/scenes:generate`, `POST /timelines/{tid}/scenes/{scene_id}/split`, `POST /timelines/{tid}/scenes/merge`, `POST /timelines/{tid}/cut-at-frame`, `PATCH /scenes/{scene_id}` (rename). `edit_scenes` dispatcht `action ∈ {generate, split, merge, cut_at_frame, rename}` auf genau diese Pfade; `args` trägt die jeweiligen Felder (`scene_id`, `frame`, `name`, ...).
  - `POST /timelines/{tid}/undo` / `.../redo`.
  - `POST /timelines/{tid}/render` → `{export_id, job_id}`; `GET /projects/{pid}/exports`; `GET /exports/{eid}`.
  - `POST /projects/{pid}/auto-overview` bzw. `POST /projects/{pid}/auto-short` mit `{"topic": topic, "target_seconds": target_seconds}`; `kind ∈ {overview, short}`.
- Tool-Beschreibungen von `edit_timeline`/`edit_scenes` tragen wörtlich: „All frames are
  integers; ranges are end-exclusive (`out_frame_exclusive`). OTIO is the source of truth —
  never edit exports, always the timeline."

- [ ] **Step 1: Failing Tests** — Fälle (gleiches Gerüst):
  editorial: `test_get_timeline_by_id_includes_history` (zwei GETs: `/timelines/t1`, `/timelines/t1/history`; Ergebnis `{"timeline": ..., "history": ...}`), `test_get_timeline_by_project_lists`, `test_edit_timeline_posts_operation_verbatim` (Body kommt byte-gleich als JSON an), `test_edit_timeline_rejects_unknown_op` (`LauraError` mit „unknown op", kein HTTP-Call), `test_edit_scenes_split_path` (`/timelines/t1/scenes/sc1/split`), `test_edit_scenes_cut_at_frame_path`, `test_timeline_undo_and_redo_paths`;
  export: `test_render_timeline_path`, `test_list_exports_path`, `test_get_export_path`, `test_auto_produce_overview_posts_topic`, `test_auto_produce_rejects_unknown_kind`.
- [ ] **Step 2: Run — FAIL, dann implementieren** (Muster Task 2; `edit_scenes` als Dispatch-Tabelle `action -> (method, path_builder)`).
- [ ] **Step 3: Run — PASS; server.py-Imports aktivieren; jetzt sind ALLE Module aus Task 1 aktiv — auskommentierte Einträge entfernen**
- [ ] **Step 4: Gates + Commit**

```bash
git add services/mcp/src/laura_mcp/tools_editorial.py services/mcp/src/laura_mcp/tools_export.py services/mcp/src/laura_mcp/server.py services/mcp/tests/test_tools_editorial.py services/mcp/tests/test_tools_export.py
git commit -m "feat(mcp): editorial family (frame-exact ops, scenes, undo) + render/export/auto_produce"
```

---

### Task 6: README + CI-Job

**Files:**
- Create: `services/mcp/README.md`
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: die bestehende Job-Struktur in `ci.yml` (der Backend-Job zeigt das uv-Setup-Muster — vor dem Editieren lesen und dessen Action-Versionen übernehmen).

- [ ] **Step 1: README schreiben** — Inhalt (vollständig, kein Auszug):
  - Was der Server ist (ein Absatz), Voraussetzung „Laura-App läuft".
  - Claude Code: `claude mcp add laura --env LAURA_TOKEN=<token> -- uv run --directory <absoluter Repo-Pfad>/services/mcp laura-mcp`
  - Claude Desktop (`claude_desktop_config.json`):
    ```json
    {
      "mcpServers": {
        "laura": {
          "command": "uv",
          "args": ["run", "--directory", "C:/Users/User/Desktop/Laura/services/mcp", "laura-mcp"],
          "env": { "LAURA_TOKEN": "<token>" }
        }
      }
    }
    ```
  - Woher der Token kommt (derselbe, mit dem die App ihr Backend startet).
  - Hinweis auf `laura_api` + Lösch-Rückfrage-Regel.
- [ ] **Step 2: ci.yml um einen Job `mcp` ergänzen** — spiegelt den bestehenden Backend-Job
  (checkout, uv-Setup wie dort), dann:
  ```yaml
  mcp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4            # Version an den bestehenden Job angleichen
      - uses: astral-sh/setup-uv@v5          # dito
      - run: uv sync --dev
        working-directory: services/mcp
      - run: uv run pytest tests/ -q
        working-directory: services/mcp
      - run: uv run mypy src tests
        working-directory: services/mcp
      - run: uv run ruff check src tests
        working-directory: services/mcp
  ```
  (CI-Lesson aus dem Repo: bare `mypy` inkl. Tests laufen lassen — deshalb `src tests`, nicht nur `src`.)
- [ ] **Step 3: Commit**

```bash
git add services/mcp/README.md .github/workflows/ci.yml
git commit -m "docs(mcp): registration README for Claude Code + Desktop; ci: mcp job"
```

---

### Task 7: Backend — BoardMeta.author, Author-Create, Follow-up-Sperre

**Files:**
- Modify: `services/local-api/src/laura/short_creator/board_models.py` (BoardMeta)
- Modify: `services/local-api/src/laura/api/short_creator.py` (`ProductionCreateRequest`, `create_production`, neue `_create_author_production_session`, Sperre in `run_production_follow_up`)
- Test: `services/local-api/tests/test_api_author_mode.py`

**Interfaces:**
- Consumes: `BoardMeta` (board_models.py, Feld-Block um `script_gate: bool = False` bei Zeile ~819), `Board.create(root, meta)` / `Board.open(root)`, `board_root_for(db, asset_id, session_id)` (production_orchestrator), `repos.create_production_session`, `_get_asset_or_404`, `_require_autoshort`, `_require_usable_agent_config`, `run_production_follow_up` (api/short_creator.py:1001).
- Produces: `BoardMeta.author: Literal["team", "external"] = "team"`;
  `_create_author_production_session(db, asset_id, *, task, target_seconds, format, language) -> str` (session_id; KEIN Job);
  `ProductionCreateRequest.author: Literal["team", "external"] = "team"`;
  409-Detail-Konstanten `_AUTHOR_SESSION_DETAIL = "author session — write via the author endpoints, not the team chat"` und (Task 8) `_TEAM_SESSION_DETAIL`.

- [ ] **Step 1: Failing Tests schreiben** — `tests/test_api_author_mode.py`, Seeding wie
  `tests/test_production_session_delete.py` (Settings/SqliteDatabase/Projekt/Asset):

```python
"""Author mode: an external session gets a board with both gates forced and no team job;
the team chat path and the author write path refuse each other's sessions."""
from __future__ import annotations

from pathlib import Path

import pytest
from fastapi import HTTPException

from laura.api.short_creator import (
    _create_author_production_session,
    run_production_follow_up,
)
from laura.config import Settings
from laura.db import repos
from laura.db.database import Database, SqliteDatabase
from laura.short_creator.board import Board
from laura.short_creator.production_orchestrator import board_root_for


def _db(tmp_path: Path) -> Database:
    settings = Settings(workspace_root=tmp_path / "ws", start_runner=False)
    db: Database = SqliteDatabase(settings.db_path)
    db.migrate()
    return db


def _seed_asset(db: Database, tmp_path: Path) -> str:
    project = repos.create_project(
        db, name="p", rate_num=30, rate_den=1, drop_frame=False,
        workspace_root=str(tmp_path / "ws" / "proj"),
    )
    asset = repos.create_asset(
        db, project_id=project["id"], type="video", display_name="a",
        source_path=str(tmp_path / "input.mp4"),
    )
    return str(asset["id"])


def test_author_create_builds_board_with_gates_and_no_job(tmp_path: Path) -> None:
    db = _db(tmp_path)
    asset_id = _seed_asset(db, tmp_path)

    session_id = _create_author_production_session(
        db, asset_id, task="ui tour short", target_seconds=35.0,
        format="insta", language="English",
    )

    session = repos.get_production_session(db, session_id)
    assert session is not None
    assert session.get("latest_job_id") in (None, "")  # no team job enqueued
    board = Board.open(board_root_for(db, asset_id, session_id))
    meta = board.meta()
    assert meta.author == "external"
    assert meta.scene_gate is True and meta.script_gate is True
    assert meta.task == "ui tour short"


def test_follow_up_refuses_author_sessions(tmp_path: Path) -> None:
    db = _db(tmp_path)
    asset_id = _seed_asset(db, tmp_path)
    session_id = _create_author_production_session(
        db, asset_id, task="t", target_seconds=30.0, format="insta", language="German",
    )

    with pytest.raises(HTTPException) as exc:
        run_production_follow_up(db, session_id, "make it punchier")
    assert exc.value.status_code == 409
    assert "author session" in str(exc.value.detail)
```

- [ ] **Step 2: Run — FAIL**

Run: `cd services/local-api && uv run pytest tests/test_api_author_mode.py -v`
Expected: FAIL (`ImportError: _create_author_production_session`)

- [ ] **Step 3: Implementieren**

1. `board_models.py`, im `BoardMeta`-Feldblock direkt unter `scene_gate`:

```python
    # Who writes the creative artifacts. "team" = the AutoGen production team (default,
    # every pre-existing board deserializes to this); "external" = an author session driven
    # through the author endpoints (MCP) — the team/message path refuses these sessions.
    author: Literal["team", "external"] = "team"
```

(`Literal` ist in board_models bereits importiert oder wird ergänzt. Defaulted → alte
`meta.json` laden unverändert; an `content_hash`/Provenienz ändert sich nichts, weil Meta
dort nicht eingeht.)

2. `api/short_creator.py`, `ProductionCreateRequest` erweitern:

```python
    # "external": create the session + board for an outside author (MCP) — both gates on,
    # no team job. "team" keeps the existing behavior byte-identical.
    author: Literal["team", "external"] = "team"
```

3. Neue Funktion neben `_create_production_session`:

```python
def _create_author_production_session(
    db: Database,
    asset_id: str,
    *,
    task: str,
    target_seconds: float,
    format: str,
    language: str,
) -> str:
    """Author-mode session: row + board synchronously, gates forced, NO production.run job.

    The board must exist before the create call returns (the author writes against it next),
    unlike the team path where run_production creates it at job time.
    """
    session_id = new_id()
    created_utc = datetime.now(UTC).isoformat(timespec="seconds")
    repos.create_production_session(
        db, session_id=session_id, asset_id=asset_id,
        created_utc=created_utc, brief_text=task,
    )
    from ..short_creator.board import Board
    from ..short_creator.board_models import BoardMeta
    from ..short_creator.production_orchestrator import board_root_for

    meta = BoardMeta(
        session_id=session_id, asset_id=asset_id, created_utc=created_utc,
        task=task, format=format, language=language,
        target_seconds=float(target_seconds),
        script_gate=True, scene_gate=True, author="external",
    )
    Board.create(board_root_for(db, asset_id, session_id), meta)
    return session_id
```

(`format`-Typ folgt `BoardMeta` — falls dort `Format`-Literal, den Parameter genauso typen.)

4. `create_production`-Endpunkt verzweigt:

```python
    if body.author == "external":
        session_id = _create_author_production_session(
            db, asset_id, task=body.task, target_seconds=float(body.target_seconds),
            format=body.format, language=body.language,
        )
        return {"session_id": session_id, "job_id": None, "board_ready": True,
                "warnings": config_warnings(resolve_from_env())}
```

(vor dem bestehenden Team-Zweig; `_require_autoshort()` und `_require_usable_agent_config()`
bleiben für BEIDE Zweige davor — der deterministische Tail nach dem Approve braucht die
Agent-Config für die QA-Stufe.)

5. Sperre in `run_production_follow_up` (api/short_creator.py:1001): nach dem Session-Lookup
Board öffnen (gleiche lokalen Imports wie oben; `FileNotFoundError`/`ValueError` → wie bisher
behandeln, also kein neuer Fehlerpfad) und:

```python
_AUTHOR_SESSION_DETAIL = "author session — write via the author endpoints, not the team chat"
...
    if board.meta().author == "external":
        raise HTTPException(status.HTTP_409_CONFLICT, _AUTHOR_SESSION_DETAIL)
```

- [ ] **Step 4: Run — PASS**

Run: `cd services/local-api && uv run pytest tests/test_api_author_mode.py -v`
Expected: 2 PASS

- [ ] **Step 5: Bestehende Suiten der berührten Module + Gates**

Run: `cd services/local-api && uv run pytest tests/test_api_scene_selection.py tests/test_production_session_delete.py -q && uv run mypy src tests && uv run ruff check src tests`
Expected: PASS/clean (Board-Meta-Erweiterung darf nichts brechen)

- [ ] **Step 6: Commit**

```bash
git add services/local-api/src/laura/short_creator/board_models.py services/local-api/src/laura/api/short_creator.py services/local-api/tests/test_api_author_mode.py
git commit -m "feat(author-mode): external author sessions — board with forced gates, no team job, follow-up lockout"
```

---

### Task 8: Backend — authoring.py Dispatch + 3 PUT-Endpunkte

**Files:**
- Create: `services/local-api/src/laura/short_creator/authoring.py`
- Modify: `services/local-api/src/laura/api/short_creator.py` (3 Endpunkte + Request-Modelle)
- Test: `services/local-api/tests/test_authoring.py`

**Interfaces:**
- Consumes: `build_production_tool_specs(db, board, *, asset_id, deps=None) -> list[ToolSpec]` (production_tools.py:1520; `ToolSpec` hat `.name`/`.description`/`.func`), `Board.open`, `board_root_for`, `repos.get_production_session`, `repos.get_job`.
- Produces: `authoring.call_production_tool(db, session_id, tool_name, /, **kwargs) -> dict[str, Any]` — wirft `HTTPException(404)` (Session/Board unbekannt), `409` (`_TEAM_SESSION_DETAIL` bzw. busy), `422` (Tool antwortete `ok: False`; `detail` = dessen `reason`/Fehlerfelder als Satz). Endpunkte `PUT /production/{sid}/scene-proposal`, `PUT /production/{sid}/storyline`, `PUT /production/{sid}/script/chapters/{chapter}`.

- [ ] **Step 1: Failing Tests**

```python
"""Author writes go through the SAME tool closures as the team — authoring.py only
resolves session→board, guards, and dispatches by name. Guard depth (capacity, grounding,
gate arming) is covered by the production_tools suites; here we prove dispatch + mapping."""
from __future__ import annotations

from pathlib import Path

import pytest
from fastapi import HTTPException

from laura.short_creator.authoring import call_production_tool
# _db/_seed_asset: gleiches Seeding-Modul-Vorspann wie tests/test_api_author_mode.py
from laura.api.short_creator import _create_author_production_session
from laura.db import repos
from laura.jobs.runner import enqueue


def test_unknown_session_404s(tmp_path: Path) -> None:
    db = _db(tmp_path)
    with pytest.raises(HTTPException) as exc:
        call_production_tool(db, "nope", "save_storyline", red_thread="x", chapters=[])
    assert exc.value.status_code == 404


def test_team_session_refuses_author_writes(tmp_path: Path) -> None:
    db = _db(tmp_path)
    asset_id = _seed_asset(db, tmp_path)
    # team-shaped board: author defaults to "team"
    from datetime import UTC, datetime
    from laura.short_creator.board import Board
    from laura.short_creator.board_models import BoardMeta
    from laura.short_creator.production_orchestrator import board_root_for
    session_id = "team-sess"
    repos.create_production_session(
        db, session_id=session_id, asset_id=asset_id,
        created_utc=datetime.now(UTC).isoformat(timespec="seconds"),
    )
    Board.create(
        board_root_for(db, asset_id, session_id),
        BoardMeta(session_id=session_id, asset_id=asset_id,
                  created_utc="2026-08-19T00:00:00+00:00", task="t", target_seconds=30.0),
    )
    with pytest.raises(HTTPException) as exc:
        call_production_tool(db, session_id, "save_storyline", red_thread="x", chapters=[])
    assert exc.value.status_code == 409
    assert "team session" in str(exc.value.detail)


def test_busy_session_409s(tmp_path: Path) -> None:
    db = _db(tmp_path)
    asset_id = _seed_asset(db, tmp_path)
    session_id = _create_author_production_session(
        db, asset_id, task="t", target_seconds=30.0, format="insta", language="German",
    )
    job_id = enqueue(db, queue="production", kind="production.run", payload={}, max_attempts=1)
    repos.set_production_session_job(db, session_id, job_id)
    with pytest.raises(HTTPException) as exc:
        call_production_tool(db, session_id, "save_storyline", red_thread="x", chapters=[])
    assert exc.value.status_code == 409


def test_tool_level_rejection_maps_to_422(tmp_path: Path) -> None:
    # No rough-cut scenes on the asset → propose_scene_selection's own block reason fires;
    # the dispatch must surface it as 422 with the tool's reason, not swallow it.
    db = _db(tmp_path)
    asset_id = _seed_asset(db, tmp_path)
    session_id = _create_author_production_session(
        db, asset_id, task="t", target_seconds=30.0, format="insta", language="German",
    )
    with pytest.raises(HTTPException) as exc:
        call_production_tool(
            db, session_id, "propose_scene_selection",
            candidates=[{"scene_number": 1, "reason": "hook"}],
        )
    assert exc.value.status_code == 422


def test_unknown_tool_name_404s(tmp_path: Path) -> None:
    db = _db(tmp_path)
    asset_id = _seed_asset(db, tmp_path)
    session_id = _create_author_production_session(
        db, asset_id, task="t", target_seconds=30.0, format="insta", language="German",
    )
    with pytest.raises(HTTPException) as exc:
        call_production_tool(db, session_id, "render_production")  # not author-callable
    assert exc.value.status_code == 404
```

- [ ] **Step 2: Run — FAIL** (`ModuleNotFoundError: authoring`)
- [ ] **Step 3: `authoring.py` implementieren**

```python
"""Author-mode dispatch: outside callers (MCP endpoints) write creative artifacts through
the SAME tool closures the AutoGen team uses — one guard source, zero duplicated logic.

Only the three creative writes are author-callable. Everything downstream of the script
gate is deterministic and runs via approve, never through this module.
"""
from __future__ import annotations

import logging
from typing import Any

from fastapi import HTTPException, status

from ..db import repos
from ..db.database import Database

logger = logging.getLogger(__name__)

_AUTHOR_CALLABLE = frozenset({"propose_scene_selection", "save_storyline", "save_script_chapter"})
_TEAM_SESSION_DETAIL = "team session — this production is written by the in-app team chat"
_BUSY_DETAIL = "a production job is running on this session — wait for it to finish"


def call_production_tool(
    db: Database, session_id: str, tool_name: str, /, **kwargs: Any
) -> dict[str, Any]:
    if tool_name not in _AUTHOR_CALLABLE:
        raise HTTPException(status.HTTP_404_NOT_FOUND, f"unknown author tool: {tool_name}")
    session = repos.get_production_session(db, session_id)
    if session is None:
        raise HTTPException(status.HTTP_404_NOT_FOUND, "session not found")
    asset_id = str(session["asset_id"])

    from .board import Board
    from .production_orchestrator import board_root_for
    from .production_tools import build_production_tool_specs

    try:
        board = Board.open(board_root_for(db, asset_id, session_id))
    except (FileNotFoundError, ValueError) as exc:
        raise HTTPException(status.HTTP_404_NOT_FOUND, "session board not found") from exc

    if board.meta().author != "external":
        raise HTTPException(status.HTTP_409_CONFLICT, _TEAM_SESSION_DETAIL)
    if _job_busy(db, session):
        raise HTTPException(status.HTTP_409_CONFLICT, _BUSY_DETAIL)

    specs = {s.name: s for s in build_production_tool_specs(db, board, asset_id=asset_id)}
    spec = specs.get(tool_name)
    if spec is None:  # defensive: the factory always builds these three
        raise HTTPException(status.HTTP_404_NOT_FOUND, f"tool not available: {tool_name}")

    result = spec.func(**kwargs)
    if not isinstance(result, dict):  # tools return dicts by contract
        raise HTTPException(status.HTTP_500_INTERNAL_SERVER_ERROR, "tool returned no result")
    if result.get("ok") is False:
        raise HTTPException(status.HTTP_422_UNPROCESSABLE_ENTITY, _reason_of(result))
    return result


def _job_busy(db: Database, session: dict[str, Any]) -> bool:
    job_id = session.get("latest_job_id")
    if not job_id:
        return False
    job = repos.get_job(db, str(job_id))
    return job is not None and str(job["status"]) in ("queued", "running")


def _reason_of(result: dict[str, Any]) -> str:
    for key in ("reason", "error", "errors"):
        value = result.get(key)
        if value:
            return str(value)[:500]
    return "the tool rejected the write"
```

- [ ] **Step 4: Endpunkte + Request-Modelle in api/short_creator.py**

```python
class SceneProposalRequest(BaseModel):
    candidates: list[dict[str, Any]] = Field(min_length=1)


class StorylineRequest(BaseModel):
    red_thread: str = Field(min_length=1)
    chapters: list[dict[str, Any]] = Field(min_length=1)


class ScriptChapterRequest(BaseModel):
    lines: list[dict[str, Any]] = Field(min_length=1)
```

```python
@router.put("/production/{session_id}/scene-proposal")
def put_scene_proposal(
    session_id: str,
    body: SceneProposalRequest,
    request: Request,
    principal: Annotated[Principal, Depends(require_permission("timeline:edit"))],
) -> dict[str, Any]:
    """Author mode: propose scenes (arms Gate S, bumps selection_version)."""
    from ..short_creator.authoring import call_production_tool

    return call_production_tool(
        _db(request), session_id, "propose_scene_selection", candidates=body.candidates
    )


@router.put("/production/{session_id}/storyline")
def put_storyline(
    session_id: str,
    body: StorylineRequest,
    request: Request,
    principal: Annotated[Principal, Depends(require_permission("timeline:edit"))],
) -> dict[str, Any]:
    """Author mode: write the storyline (window references validated by the tool core)."""
    from ..short_creator.authoring import call_production_tool

    return call_production_tool(
        _db(request), session_id, "save_storyline",
        red_thread=body.red_thread, chapters=body.chapters,
    )


@router.put("/production/{session_id}/script/chapters/{chapter}")
def put_script_chapter(
    session_id: str,
    chapter: int,
    body: ScriptChapterRequest,
    request: Request,
    principal: Annotated[Principal, Depends(require_permission("timeline:edit"))],
) -> dict[str, Any]:
    """Author mode: write one script chapter (capacity guard replies exactly as in the team path)."""
    from ..short_creator.authoring import call_production_tool

    return call_production_tool(
        _db(request), session_id, "save_script_chapter", chapter=chapter, lines=body.lines
    )
```

- [ ] **Step 5: Run — alle neuen Tests PASS; danach volle Gates**

Run: `cd services/local-api && uv run pytest tests/test_authoring.py tests/test_api_author_mode.py -v && uv run mypy src tests && uv run ruff check src tests`
Expected: PASS/clean

- [ ] **Step 6: Commit**

```bash
git add services/local-api/src/laura/short_creator/authoring.py services/local-api/src/laura/api/short_creator.py services/local-api/tests/test_authoring.py
git commit -m "feat(author-mode): authoring dispatch through team tool closures + scene-proposal/storyline/script endpoints"
```

---

### Task 9: Backend — approve-Service extrahieren + Endpunkt + Executor-Refactor

**Files:**
- Modify: `services/local-api/src/laura/api/short_creator.py` (Service + Endpunkt)
- Modify: `services/local-api/src/laura/chat/executor.py` (`_handle_approve_script` ruft den Service)
- Test: `services/local-api/tests/test_api_author_mode.py` (erweitern)

**Interfaces:**
- Consumes: die exakte Approve-Semantik aus `chat/executor.py:463-578` (content-hash-Bindung, `already_current`, Busy-Check VOR Stamp, Write-then-enqueue mit kompensierendem `clear_script_approval`, `run_production_resume`).
- Produces in `api/short_creator.py`:

```python
_NO_SCRIPT_DETAIL = "no script to approve yet — the production pauses at the gate by itself"
_APPROVE_BUSY_DETAIL = "a production job is running on this session — wait for it to finish"

def approve_production_script(db: Database, session_id: str) -> dict[str, Any]:
    """Outcome dict: {"outcome": "resumed", "session_id", "job_id"} on a fresh or
    re-triggered approval; {"outcome": "already_done", "session_id"} when the stamp is
    content-current AND the board is finished. Raises HTTPException 404 (unknown
    session/board), 409 (_NO_SCRIPT_DETAIL / _APPROVE_BUSY_DETAIL)."""
```

- Executor-Mapping danach: 404 → `_NO_SESSION_TEXT`, 409 mit `_NO_SCRIPT_DETAIL` → `_NO_SCRIPT_TO_APPROVE_TEXT`, 409 mit `_APPROVE_BUSY_DETAIL` → `_RUN_BUSY_TEXT`, `already_done` → `_SCRIPT_ALREADY_APPROVED_TEXT`, `resumed` → bestehende Action-Card. Die pinnenden Executor-/Chat-Tests bleiben unverändert grün — das ist das Refactor-Erfolgskriterium.

- [ ] **Step 1: Failing Tests ergänzen** (in `test_api_author_mode.py`; Seeding wie gehabt, Script aufs Board schreiben):

```python
def _board_with_script(db, tmp_path):  # helper: author session + minimal approved-able script
    asset_id = _seed_asset(db, tmp_path)
    session_id = _create_author_production_session(
        db, asset_id, task="t", target_seconds=30.0, format="insta", language="German",
    )
    from laura.short_creator.board import Board
    from laura.short_creator.board_models import Script, ScriptLine
    from laura.short_creator.production_orchestrator import board_root_for
    board = Board.open(board_root_for(db, asset_id, session_id))
    board.save("script", Script(
        language="German",
        lines=[ScriptLine(chapter=1, scene_number=1, text="Willkommen bei Laura.")],
    ))
    return session_id, board


def test_approve_stamps_and_enqueues_resume(tmp_path, monkeypatch) -> None:
    db = _db(tmp_path)
    session_id, board = _board_with_script(db, tmp_path)
    import laura.api.short_creator as api_sc
    calls: list[str] = []
    monkeypatch.setattr(api_sc, "run_production_resume",
                        lambda db_, sid: calls.append(sid) or {"job_id": "j1"})
    out = api_sc.approve_production_script(db, session_id)
    assert out["outcome"] == "resumed" and out["job_id"] == "j1"
    assert calls == [session_id]
    assert board.meta().script_approved_utc is not None


def test_approve_without_script_409s(tmp_path) -> None:
    db = _db(tmp_path)
    asset_id = _seed_asset(db, tmp_path)
    session_id = _create_author_production_session(
        db, asset_id, task="t", target_seconds=30.0, format="insta", language="German",
    )
    import laura.api.short_creator as api_sc
    with pytest.raises(HTTPException) as exc:
        api_sc.approve_production_script(db, session_id)
    assert exc.value.status_code == 409


def test_failed_resume_rolls_back_fresh_stamp(tmp_path, monkeypatch) -> None:
    db = _db(tmp_path)
    session_id, board = _board_with_script(db, tmp_path)
    import laura.api.short_creator as api_sc

    def boom(db_, sid):
        raise HTTPException(500, "queue down")

    monkeypatch.setattr(api_sc, "run_production_resume", boom)
    with pytest.raises(HTTPException):
        api_sc.approve_production_script(db, session_id)
    assert board.meta().script_approved_utc is None  # compensating rollback
```

(`Board.save(name, artifact)` und die `Script`/`ScriptLine`-Formen sind gegen
`board.py:374` bzw. `board_models.py:236/360` verifiziert — `ScriptLine.text` muss
sprechbar sein, also schlichter Prosa-Satz ohne führende Ziffern.)

- [ ] **Step 2: Run — FAIL** (`AttributeError: approve_production_script`)
- [ ] **Step 3: Service implementieren** — Logik 1:1 aus `_handle_approve_script`
  (executor.py:529-567) übertragen, chat-frei:

```python
def approve_production_script(db: Database, session_id: str) -> dict[str, Any]:
    session = repos.get_production_session(db, session_id)
    if session is None:
        raise HTTPException(status.HTTP_404_NOT_FOUND, "session not found")
    asset_id = str(session["asset_id"])

    from ..short_creator.board import Board
    from ..short_creator.board_models import Script, content_hash
    from ..short_creator.production_orchestrator import board_root_for

    try:
        board = Board.open(board_root_for(db, asset_id, session_id))
    except (FileNotFoundError, ValueError) as exc:
        raise HTTPException(status.HTTP_404_NOT_FOUND, "session board not found") from exc

    script = board.load("script")
    if not isinstance(script, Script):
        raise HTTPException(status.HTTP_409_CONFLICT, _NO_SCRIPT_DETAIL)
    current_hash = content_hash(script)
    meta = board.meta()
    already_current = (
        meta.script_approved_utc is not None
        and meta.script_approved_script_hash == current_hash
    )
    if already_current and board.resume_point(_expected_scenes_for(db, asset_id)) == "done":
        return {"outcome": "already_done", "session_id": session_id}

    from ..chat.executor import _production_job_busy  # single busy-definition, reused

    if _production_job_busy(db, session):
        raise HTTPException(status.HTTP_409_CONFLICT, _APPROVE_BUSY_DETAIL)

    now_utc = datetime.now(UTC).isoformat(timespec="seconds")
    if not already_current:
        board.set_script_approved(now_utc, current_hash)
    try:
        result = run_production_resume(db, session_id)
    except HTTPException:
        if not already_current:
            board.clear_script_approval()
        raise
    except Exception:
        if not already_current:
            board.clear_script_approval()
        raise
    return {"outcome": "resumed", "session_id": session_id, "job_id": result["job_id"]}
```

(Wenn der `chat.executor`-Import einen Zyklus erzeugt — executor importiert bereits aus
`api.short_creator` —, dann `_production_job_busy` stattdessen nach `api/short_creator.py`
verschieben und den Executor von DORT importieren lassen; eine Definition, egal in welchem
der beiden Module.)

Endpunkt:

```python
@router.post("/production/{session_id}/script:approve", status_code=status.HTTP_202_ACCEPTED)
def approve_script_endpoint(
    session_id: str,
    request: Request,
    principal: Annotated[Principal, Depends(require_permission("timeline:edit"))],
) -> dict[str, Any]:
    """Approve the current script (content-hash-bound) and run the deterministic tail."""
    return approve_production_script(_db(request), session_id)
```

- [ ] **Step 4: Executor-Refactor** — `_handle_approve_script` behält Session-Resolution und
  Karten-/Text-Erzeugung, ersetzt aber seinen Board-Block durch den Service-Aufruf mit dem
  im Interfaces-Block definierten Mapping (Detail-Konstanten importieren und per `==`
  matchen). Die bestehenden Chat-Texte (`_SCRIPT_ALREADY_APPROVED_TEXT` usw.) bleiben
  byte-identisch.
- [ ] **Step 5: Run — neue Tests PASS + die bestehende Executor-Suite unverändert grün**

Run: `cd services/local-api && uv run pytest tests/test_api_author_mode.py tests/ -k "approve" -v`
Expected: PASS — insbesondere jeder bestehende approve-Test des Chat-Pfads.

- [ ] **Step 6: Volle Gates + Commit**

Run: `cd services/local-api && uv run mypy src tests && uv run ruff check src tests`

```bash
git add services/local-api/src/laura/api/short_creator.py services/local-api/src/laura/chat/executor.py services/local-api/tests/test_api_author_mode.py
git commit -m "feat(author-mode): approve service shared by chat executor and new script:approve endpoint"
```

---

### Task 10: MCP Produktions-Familie (7 Tools)

**Files:**
- Create: `services/mcp/src/laura_mcp/tools_production.py`
- Modify: `services/mcp/src/laura_mcp/server.py`
- Test: `services/mcp/tests/test_tools_production.py`

**Interfaces:**
- Consumes: Task-1-Muster; die Backend-Endpunkte aus Tasks 7-9 plus bestehende (`GET /production/{sid}`, `GET /production/{sid}/events`, `POST /production/{sid}/scene-selection:confirm`).
- Produces: `start_production(asset_id, task, target_seconds=60, format="insta", language="German")` (Body enthält `"author": "external"`), `production_status(session_id)` (GET Status + Events-Tail, Ergebnis `{"status": ..., "events": ...}`), `propose_scenes(session_id, candidates)`, `confirm_scenes(session_id, scene_numbers, selection_version)`, `save_storyline(session_id, red_thread, chapters)`, `save_script_chapter(session_id, chapter, lines)`, `approve_script(session_id)`.

- [ ] **Step 1: Failing Tests** — Gerüst wie Task 2; Fälle:
  `test_start_production_sends_author_external` (Body enthält `"author": "external"` + task/target_seconds/format/language),
  `test_production_status_merges_status_and_events` (zwei GETs `/production/s1` + `/production/s1/events`),
  `test_propose_scenes_put_path` (`PUT /production/s1/scene-proposal`, Body `{"candidates": [...]}`),
  `test_confirm_scenes_posts_selection_version` (Body `{"scene_numbers": [1, 3], "selection_version": 2}` auf `/production/s1/scene-selection:confirm`),
  `test_save_storyline_put`, `test_save_script_chapter_puts_chapter_number` (Pfad `/production/s1/script/chapters/2`), `test_approve_script_post` (`/production/s1/script:approve`).
- [ ] **Step 2: Run — FAIL, dann implementieren.** Tool-Docstrings tragen den Ablauf-Vertrag
  (Kurzform des Skills, für Claude Desktop): propose → user confirms → storyline →
  script chapters → approve; „the script must be grounded in the transcript of the chosen
  scenes; approve starts voice→cutlist→contact sheet→render automatically."
- [ ] **Step 3: Run — PASS; server.py-Import aktivieren**
- [ ] **Step 4: Gates + Commit**

```bash
git add services/mcp/src/laura_mcp/tools_production.py services/mcp/src/laura_mcp/server.py services/mcp/tests/test_tools_production.py
git commit -m "feat(mcp): production family — author-mode session, gates, script, approve"
```

---

### Task 11: laura-producer-Skill + Live-Prüfliste

**Files:**
- Create: `.claude/skills/laura-producer/SKILL.md`
- Modify: `tasks/todo.md` (Arc-Eintrag + manuelle Prüfliste)

- [ ] **Step 1: SKILL.md schreiben** — Frontmatter + Vertrag:

```markdown
---
name: laura-producer
description: Drive a Laura video production end-to-end over the laura MCP server — scene gate, storyline, grounded script, approve, deterministic render. Use when the user asks to produce/cut/export a video via Laura from this session.
---

# Laura Producer

Preconditions: the Laura desktop app is running (every tool fails with "Laura app is not
running" otherwise). All writes go through the MCP tools — never edit workspace files.

## Production contract (order is mandatory)
1. `list_assets` / `get_transcript` / `get_shots_and_scenes` — read before you write.
2. `start_production(asset_id, task, …)` — creates an author session (gates armed).
3. `propose_scenes` with candidates you justify from transcript + rough-cut scenes.
   THE USER picks: present the proposal, wait for their answer, then `confirm_scenes`
   with the `selection_version` from `production_status` (a changed proposal 409s — re-read).
4. `save_storyline` — chapters reference scene windows; only confirmed scenes.
5. `save_script_chapter` per chapter. Grounding rule: every claim comes from the
   transcript of the chosen material; never invent facts. The capacity guard measures
   speech rate from real timings — respect its rejections, shorten instead of arguing.
6. Show the user the script; on their explicit yes call `approve_script`. Everything
   after (voice → cutlist → contact sheet → render → QA) is deterministic and automatic —
   watch it via `production_status` / `job_status`, show `get_contact_sheet` when ready.

## Rules
- Language follows the user's instruction language (task + script in the same language).
- Look before you cut: `get_frame` around any boundary you are unsure about.
- Deletes (productions, assets, projects) only via `laura_api` and only after the user
  confirmed in this conversation. The input footage is never deleted by a production delete.
- If a tool answers 409 "team session", this production belongs to the in-app chat — do
  not write to it; offer to start a fresh author session instead.
```

- [ ] **Step 2: tasks/todo.md ergänzen** — neuer Abschnitt „Laura-MCP (2026-08-19)" mit
  Verweis auf Spec/Plan und der manuellen Live-Prüfliste:
  1. App starten, MCP registrieren (README), `list_projects` antwortet.
  2. `import_media` mit einer echten URL → `import_status` bis online → `analyze_asset`.
  3. Voller Author-Durchlauf bis abspielbarem Export; parallel in der App sichtbar
     (Open Productions, Contact Sheet, Export-Tab).
  4. Editorial-Stichprobe: `edit_timeline` trim + `timeline_undo` + `render_timeline`.
  5. Claude Desktop: Registrierung, `list_projects` + `get_frame` (Bild sichtbar).
- [ ] **Step 3: Commit**

```bash
git add .claude/skills/laura-producer/SKILL.md tasks/todo.md
git commit -m "docs: laura-producer skill (production contract) + live checklist for the MCP arc"
```
